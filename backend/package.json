{
  "name": "meterx-backend",
  "version": "1.0.0",
  "private": true,
  "description": "MongoDB-backed sync API for the MeterX Android app",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "db:init": "node src/initDatabase.js",
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
